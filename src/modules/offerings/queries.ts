/**
 * `offerings` read-only queries — owned by P3 (master plan §3 ownership map). Intentionally empty in P2.8:
 * Queries are `definePublicAction` / `defineAction` reads that never mutate.
 */
export {};
